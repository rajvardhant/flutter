import 'package:flutter/material.dart';
class Malik extends StatelessWidget {
  const Malik({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Malik'),
        centerTitle: true,
        backgroundColor: Colors.blue,
      ),
    );
  }
}
